[package]
name = "devscout-cleaner"
version = "0.1.0"
edition = "2021"
description = "Conservative Windows folder and registry cleanup utility"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"

